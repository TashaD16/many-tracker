# Money Tracker - Личный финансовый мониторинг

Веб-приложение для мониторинга личных финансов с полной поддержкой управления доходами, расходами, бюджетирования и работы с несколькими валютами.

## Технологии

### Backend
- **Node.js** с **Express.js** - REST API
- **PostgreSQL** / **Supabase** - база данных
- **Prisma** - ORM
- **JWT** - аутентификация
- **WebSocket** (ws) - реал-тайм обновления
- **bcryptjs** - хеширование паролей
- **express-validator** - валидация данных
- **helmet** - безопасность
- **Chart.js** - графики
- **PDFKit** и **csv-writer** - экспорт отчетов

### Frontend
- **React.js** - UI фреймворк
- **Material-UI (MUI)** - компоненты UI
- **Chart.js + react-chartjs-2** - графики и диаграммы
- **React Router** - маршрутизация
- **Axios** - HTTP клиент
- **date-fns** - работа с датами

## Функциональность

### Основные функции

1. **Управление транзакциями**
   - Добавление/редактирование/удаление доходов и расходов
   - Фильтрация по дате, типу, категории, счету
   - Сортировка по дате или сумме

2. **Управление категориями**
   - Предопределенные и настраиваемые категории
   - Цветовая кодировка
   - Поддержка иконок

3. **Управление счетами**
   - Несколько счетов (наличные, карты, сбережения)
   - Поддержка нескольких валют (BYN, USD, EUR, RUB)
   - Автоматическое обновление балансов

4. **Переводы между счетами**
   - Переводы с автоматической конвертацией валют
   - Учет курсов валют

5. **Бюджетирование**
   - Установка лимитов на месяц/квартал/год
   - Отслеживание прогресса по категориям
   - Визуализация прогресса

6. **Дашборд**
   - Общий баланс
   - Сводки по доходам/расходам
   - Круговые диаграммы по категориям
   - Недавние транзакции
   - Прогресс по бюджетам

7. **Работа с валютами**
   - Обновление курсов валют через API myfin.by
   - Конвертер валют
   - Автоматическая конвертация при переводах

8. **Экспорт отчетов**
   - Экспорт в CSV
   - Экспорт в PDF

## Установка и запуск

### Требования

- Node.js (v16 или выше)
- PostgreSQL (v12 или выше)
- npm или yarn

### Backend

1. Перейдите в папку backend:
```bash
cd backend
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env`:
```bash
DATABASE_URL="postgresql://user:password@localhost:5432/money_tracker?schema=public"
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000
MYFIN_API_URL=https://api.myfin.by/bank/kursExchange
```

4. Настройте базу данных:

   **Вариант A: Локальный PostgreSQL**
   - Установите PostgreSQL
   - Создайте базу данных: `CREATE DATABASE money_tracker;`
   - Обновите `DATABASE_URL` в `.env`

   **Вариант B: Supabase (рекомендуется)**
   - Создайте проект на https://supabase.com
   - Получите connection string из Settings → Database
   - Обновите `DATABASE_URL` в `.env`:
     ```
     DATABASE_URL="postgresql://postgres:ваш_пароль@db.xxxxx.supabase.co:5432/postgres?schema=public"
     ```
   - 📖 **Подробные инструкции:** см. [SUPABASE_SETUP.md](./SUPABASE_SETUP.md)

5. Запустите миграции Prisma:
```bash
npx prisma generate
npx prisma migrate dev --name init
```

6. (Опционально) Заполните базу тестовыми данными:
```bash
npm run seed
```

7. Запустите сервер:
```bash
# Режим разработки
npm run dev

# Или продакшн
npm start
```

Backend будет доступен на `http://localhost:5000`

### Frontend

1. Перейдите в папку frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите приложение:
```bash
npm start
```

Frontend будет доступен на `http://localhost:3000`

## Тестирование

### Backend тесты

```bash
cd backend
npm test
```

### Frontend тесты

```bash
cd frontend
npm test
```

## Деплой

### Heroku

#### Backend

1. Создайте приложение на Heroku:
```bash
heroku create money-tracker-api
```

2. Добавьте PostgreSQL:
```bash
heroku addons:create heroku-postgresql:hobby-dev
```

3. Установите переменные окружения:
```bash
heroku config:set JWT_SECRET=your-production-secret
heroku config:set NODE_ENV=production
heroku config:set FRONTEND_URL=https://your-frontend-url.vercel.app
```

4. Запустите миграции:
```bash
heroku run npx prisma migrate deploy
```

5. Задеплойте:
```bash
git push heroku main
```

#### Frontend

1. Создайте файл `.env.production`:
```
REACT_APP_API_URL=https://your-backend-url.herokuapp.com
```

2. Задеплойте на Vercel или Netlify:
```bash
npm run build
# Загрузите папку build на хостинг
```

### Vercel

1. Установите Vercel CLI:
```bash
npm i -g vercel
```

2. Задеплойте frontend:
```bash
cd frontend
vercel
```

3. Для backend создайте `vercel.json`:
```json
{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/server.js"
    }
  ]
}
```

## Безопасность

- ✅ Пароли хешируются с помощью bcryptjs
- ✅ JWT токены для аутентификации
- ✅ Helmet для защиты от XSS и других атак
- ✅ Rate limiting для API endpoints
- ✅ Валидация данных на сервере
- ✅ SQL инъекции защищены через Prisma ORM
- ✅ CORS настроен для работы с frontend

## Структура проекта

```
money-tracker/
├── backend/
│   ├── routes/          # API маршруты
│   ├── middleware/      # Middleware (auth, validation)
│   ├── prisma/          # Prisma схемы и миграции
│   ├── __tests__/       # Тесты
│   ├── server.js        # Точка входа
│   └── package.json
├── frontend/
│   ├── public/          # Статические файлы
│   ├── src/
│   │   ├── components/  # React компоненты
│   │   ├── pages/       # Страницы приложения
│   │   ├── context/     # React Context (Auth)
│   │   ├── App.js       # Главный компонент
│   │   └── index.js     # Точка входа
│   └── package.json
└── README.md
```

## API Endpoints

### Authentication
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Получить текущего пользователя

### Transactions
- `GET /api/transactions` - Получить транзакции (с фильтрами)
- `POST /api/transactions` - Создать транзакцию
- `PUT /api/transactions/:id` - Обновить транзакцию
- `DELETE /api/transactions/:id` - Удалить транзакцию

### Categories
- `GET /api/categories` - Получить категории
- `POST /api/categories` - Создать категорию
- `PUT /api/categories/:id` - Обновить категорию
- `DELETE /api/categories/:id` - Удалить категорию

### Accounts
- `GET /api/accounts` - Получить счета
- `POST /api/accounts` - Создать счет
- `PUT /api/accounts/:id` - Обновить счет
- `DELETE /api/accounts/:id` - Удалить счет

### Transfers
- `GET /api/transfers` - Получить переводы
- `POST /api/transfers` - Создать перевод

### Budgets
- `GET /api/budgets` - Получить бюджеты
- `POST /api/budgets` - Создать бюджет
- `PUT /api/budgets/:id` - Обновить бюджет
- `DELETE /api/budgets/:id` - Удалить бюджет

### Currencies
- `GET /api/currencies/rates` - Получить курсы валют
- `POST /api/currencies/rates/update` - Обновить курсы
- `POST /api/currencies/convert` - Конвертировать валюту

### Dashboard
- `GET /api/dashboard` - Получить данные дашборда

### Reports
- `GET /api/reports/export/csv` - Экспорт в CSV
- `GET /api/reports/export/pdf` - Экспорт в PDF

## Лицензия

MIT

## Поддержка

Для вопросов и предложений создайте issue в репозитории проекта.
