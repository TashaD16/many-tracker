# Деплой на Vercel - Краткая инструкция

## 🚀 Быстрый старт

### Вариант 1: Через Vercel Dashboard (Рекомендуется)

1. **Подготовьте репозиторий:**
   ```bash
   git add .
   git commit -m "Ready for Vercel deployment"
   git push
   ```

2. **Импортируйте проект:**
   - Откройте https://vercel.com
   - Нажмите "Add New Project"
   - Выберите ваш GitHub репозиторий
   - Настройте:
     - **Framework Preset**: `Create React App`
     - **Root Directory**: `frontend`
     - **Build Command**: `npm run build` (по умолчанию)
     - **Output Directory**: `build` (по умолчанию)

3. **Добавьте переменные окружения:**
   ```
   REACT_APP_SUPABASE_URL = https://xxxxx.supabase.co
   REACT_APP_SUPABASE_ANON_KEY = ваш_публичный_ключ
   ```

4. **Деплой:**
   - Нажмите "Deploy"
   - Готово! 🎉

### Вариант 2: Через Vercel CLI

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите
vercel login

# Перейдите в frontend
cd frontend

# Деплой
vercel

# Production деплой
vercel --prod
```

## 📋 Что настроено

✅ **Конфигурация Vercel:**
- `frontend/vercel.json` - настройки для Create React App
- `frontend/public/_redirects` - редиректы для SPA
- `.vercelignore` - файлы, которые не нужно деплоить

✅ **Оптимизация:**
- Кэширование статических файлов
- Автоматическая оптимизация изображений
- CDN для быстрой загрузки

## 🔧 Переменные окружения

В Vercel Dashboard → Settings → Environment Variables:

### Production
```
REACT_APP_SUPABASE_URL=https://xxxxx.supabase.co
REACT_APP_SUPABASE_ANON_KEY=ваш_публичный_ключ
```

### Preview (опционально)
Можно использовать тестовые ключи для preview окружений.

## 🌐 Домен

После деплоя ваш сайт будет доступен по адресу:
`https://your-project.vercel.app`

### Настройка кастомного домена:
1. Vercel Dashboard → Settings → Domains
2. Добавьте ваш домен
3. Следуйте инструкциям для настройки DNS

## 🔄 Continuous Deployment

После подключения GitHub:
- Каждый push в `main` → автоматический production деплой
- Pull Request → preview деплой для тестирования

## 📊 Мониторинг

Vercel предоставляет:
- **Analytics** - статистика посещений
- **Speed Insights** - метрики производительности
- **Logs** - логи приложения
- **Real-time monitoring** - мониторинг в реальном времени

## 🐛 Troubleshooting

### Ошибка сборки
- Проверьте логи в Vercel Dashboard
- Убедитесь, что `npm run build` работает локально
- Проверьте все зависимости в `package.json`

### Supabase не работает
- Проверьте переменные окружения в Vercel
- Убедитесь, что используете anon key (не service_role)
- Проверьте CORS настройки в Supabase

### Стили не применяются
- Убедитесь, что `index.css` импортирован в `index.js`
- Проверьте конфигурацию Tailwind
- Очистите кэш и пересоберите

### Роутинг не работает
- Убедитесь, что `_redirects` файл в `public/`
- Проверьте настройки rewrites в `vercel.json`

## 📚 Дополнительная информация

- Подробная инструкция: `VERCEL_DEPLOY.md`
- Быстрый деплой: `QUICK_DEPLOY_VERCEL.md`
- Официальная документация: https://vercel.com/docs

## ✅ Готово!

После деплоя ваш Money Tracker будет доступен в интернете! 🚀
