# Быстрый деплой на Vercel

## Через Vercel Dashboard (5 минут)

### 1. Подготовьте репозиторий
```bash
git add .
git commit -m "Ready for Vercel"
git push
```

### 2. Импортируйте проект
1. Откройте https://vercel.com
2. Нажмите "Add New Project"
3. Выберите ваш GitHub репозиторий
4. Настройте:
   - **Framework Preset**: `Create React App`
   - **Root Directory**: `frontend`
   - **Build Command**: `npm run build` (по умолчанию)
   - **Output Directory**: `build` (по умолчанию)

### 3. Добавьте переменные окружения
В разделе "Environment Variables" добавьте:
```
REACT_APP_SUPABASE_URL = https://xxxxx.supabase.co
REACT_APP_SUPABASE_ANON_KEY = ваш_публичный_ключ
```

### 4. Деплой
Нажмите "Deploy" - готово! 🚀

## Через Vercel CLI

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите
vercel login

# Перейдите в frontend
cd frontend

# Деплой
vercel

# Production деплой
vercel --prod
```

## После деплоя

Ваш сайт будет доступен по адресу:
`https://your-project.vercel.app`

## Автоматический деплой

После первого деплоя, каждый push в `main` ветку автоматически деплоится на production.

## Проверка

1. Откройте ваш сайт
2. Проверьте, что переменные окружения работают
3. Протестируйте регистрацию/вход
4. Проверьте работу с Supabase

## Troubleshooting

**Ошибка сборки?**
- Проверьте логи в Vercel Dashboard
- Убедитесь, что `npm run build` работает локально

**Supabase не работает?**
- Проверьте переменные окружения
- Убедитесь, что используете anon key (не service_role)

**Стили не применяются?**
- Проверьте, что Tailwind правильно настроен
- Убедитесь, что `index.css` импортирован
